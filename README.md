# NexPay Frontend (Phase 1)

## Setup

From the project root:

```bash
npm install
npm run dev
```

The frontend expects the backend at `http://localhost:8000/api`.

## Routes

- `/register`
- `/login`
- `/dashboard`
- `/event/:id`

